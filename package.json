{
  "name": "certificate-validator",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "start": "turbo start"
  },
  "devDependencies": {
    "turbo": "latest"
  },

  "engines": {
    "node": ">=18"
  }
}
